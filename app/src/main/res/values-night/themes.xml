<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Dark Mode -->
    <style name="Base.Theme.David" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- 1. Status Bar: Fixed Black -->
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- 2. Surface Colors for Dark Mode -->
        <item name="colorSurface">#121212</item>
        <item name="colorSurfaceVariant">#2C2C2C</item>
        <item name="colorOnSurface">@android:color/white</item>
        <item name="colorOnSurfaceVariant">#B0B0B0</item>
        <item name="colorError">#CF6679</item>

        <!-- 3. FAB Colors for DARK Mode: White background, Black icon -->
        <item name="colorFloatingActionButton">@android:color/white</item>
        <item name="colorOnFloatingActionButton">@android:color/black</item>

    </style>
</resources>